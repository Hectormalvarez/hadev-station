---
# ------------------------------------------------------------------------------
# KVM / QEMU Stack
# ------------------------------------------------------------------------------
- name: Install KVM/QEMU Packages
  become: true
  apt:
    name: "{{ kvm_packages }}"
    state: present
    update_cache: yes

# ------------------------------------------------------------------------------
# HashiCorp Tooling Setup (Packer & Vagrant)
# ------------------------------------------------------------------------------
- name: Ensure APT Keyrings Directory Exists
  become: true
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'

- name: Download HashiCorp Official GPG Key
  become: true
  get_url:
    url: https://apt.releases.hashicorp.com/gpg
    dest: /etc/apt/keyrings/hashicorp.asc
    mode: '0644'
    # Always re-download so rotated signing keys don't go stale (NO_PUBKEY failures)
    force: true

- name: Add HashiCorp Repository
  become: true
  apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/hashicorp.asc] https://apt.releases.hashicorp.com {{ ansible_facts['distribution_release'] }} main"
    state: present

- name: Install Packer and Vagrant
  become: true
  apt:
    name:
      - packer
      - vagrant
    state: present
    update_cache: yes

- name: Check if Vagrant-Libvirt Plugin is Installed
  command: vagrant plugin list
  register: vagrant_plugins
  changed_when: false
  become: false

- name: Install Vagrant-Libvirt Build Dependencies
  become: true
  apt:
    name: "{{ vagrant_libvirt_deps }}"
    state: present
  when: "'vagrant-libvirt' not in vagrant_plugins.stdout"

- name: Install Vagrant-Libvirt Plugin Natively
  command: vagrant plugin install vagrant-libvirt
  when: "'vagrant-libvirt' not in vagrant_plugins.stdout"
  become: false